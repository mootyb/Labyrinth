"use strict"
import { Meteor } from 'meteor/meteor';
import { Accounts } from 'meteor/accounts-base';
import { FilesCollection } from 'meteor/ostrio:files';
//import {Shows} from "../lib/schemas/collections.js";

// window.Shows = Shows;
Meteor.startup(() => {
  // code to run on server at startup
});

